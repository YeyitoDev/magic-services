app = "magic-services"

[build]
  dockerfile = "Dockerfile"

[env]
  ENVIRONMENT = "testing"
  PYTHONUNBUFFERED = "1"

[[vm]]
  memory = "512mb"
  cpu_kind = "shared"
  cpus = 1

[processes]
  app = "python main.py"
